# Historical Commit Code Snapshots

This folder is reserved for ad hoc code snapshots that need to be kept outside
the main rerun shortlist.

Requested commit prefixes:

- `4d73a93`
- `cac9c40`
- `2c8382d`
- `13bf6a5`

Status in the current local checkout:

- `git rev-list --all` does not resolve these prefixes
- `git reflog --all` does not show these prefixes
- remote GitHub commit search also returned no matches for these prefixes

Until the exact full commit SHA or branch source is provided, each requested
prefix is kept as a placeholder directory so later snapshots can be dropped in
without changing the branch layout again.
